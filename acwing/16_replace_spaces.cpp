// acwing 16_replace_spaces
/*class Solution {
public:
    string replaceSpaces(string &str) {
        string res;
        for (int i = 0; i < str.size(); i++) { 
            if (str[i] != ' ') {
                res += str[i];
            } else {
                res += "%20";
            }
        }
        return res;
    }
};
*/